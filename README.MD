# Beam Accelerator Simulator

# Overview

This programm models the destribution of a beam of charged particles through a magnetic lattice using transfer matrixes and Twiss parameters
'''bash
# installing dependencies
pip install -r requirements.txt

# starting initial demo
streamlit run demos/streamlit_app.py

# starting tests
python -m pytest tests/

# quick demo
python run_demo.py

# team
[name] - tasks
[name] - tasks
[name] - tasks
[name] - tasks
